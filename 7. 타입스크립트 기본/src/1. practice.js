"use strict";
const message = "hello world";
console.log(message);
let count = 0; // 타입 생략 가능
count += 1;
// count = "문자열"; // 에러
const msg = "hello world";
const done = false;
const numbers = [1, 2, 3];
const msgs = ["hello", "world"];
let mightBeUndefined = undefined; // string 혹은 undefined 가능
let nullableNumber = null; // or 숫자
let color = "red";
color = "yellow";
// color = "green"; // 에러
